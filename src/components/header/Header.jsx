import {Navbar} from "../navbar"
import HeaderTop from "./HeaderTop"

export const Header = () => {
  return (
    <header>
      <HeaderTop/>
      <Navbar/>
    </header>
  )
}

export default Header